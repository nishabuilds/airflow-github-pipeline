version: 2

models:
  - name: stg_issues
    columns:
      - name: issue_id
        tests: [unique, not_null]
      - name: created_at
        tests: [not_null]
      - name: state
        tests:
          - accepted_values:
              arguments:
                values: ['open', 'closed']
