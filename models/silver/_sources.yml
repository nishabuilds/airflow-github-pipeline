version: 2

sources:
  - name: bronze
    catalog: gh
    schema: bronze
    tables:
      - name: raw_airflow_issues
        config:
          loaded_at_field: ingested_at
          freshness:
            warn_after: {count: 24, period: hour}
